{
  "name": "icebreaker-app",
  "version": "1.0.0",
  "description": "MVP Icebreaker application for team meetings",
  "scripts": {
    "server": "cd backend && node server.js",
    "client": "cd frontend && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["icebreaker", "meetings", "team-building"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
